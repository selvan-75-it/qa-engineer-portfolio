# SQL Test Execution Report

## Project Information

| Field | Details |
|---|---|
| Project | QA Engineer Internship Portfolio |
| Database | SQLite Practice E-Commerce Database |
| Testing Type | Database / SQL Testing |
| Tester | Selvan L |
| Execution | Local practice database |

## Execution Results

| Test Case | Description | Result | Observation |
|---|---|---|---|
| TC-SQL-001 | Verify product count | **PASS** | Expected 6, Actual 6 |
| TC-SQL-004 | Verify no negative product prices | **PASS** | Expected 0 rows, Actual 0 rows |
| TC-SQL-007 | Verify product price ascending order | **PASS** | Expected ['Mouse', 'Keyboard', 'Headphones', 'Office Chair', 'Desk', 'Laptop'], Actual ['Mouse', 'Keyboard', 'Headphones', 'Office Chair', 'Desk', 'Laptop'] |
| TC-SQL-008 | Verify orders reference valid users | **PASS** | Expected 0 orphan orders, Actual 0 |
| TC-SQL-010 | Verify order items reference valid products | **PASS** | Expected 0 orphan items, Actual 0 |

## Executed SQL Checks

### TC-SQL-001 — Product Count

```sql
SELECT COUNT(*) FROM products;
```

Expected: 6 products.

Actual: 6 products.

Result: **PASS**

### TC-SQL-004 — Negative Product Prices

```sql
SELECT *
FROM products
WHERE price < 0;
```

Expected: 0 rows.

Actual: 0 rows.

Result: **PASS**

### TC-SQL-007 — Product Price Sorting

```sql
SELECT name, price
FROM products
ORDER BY price ASC;
```

Expected order:

1. Mouse — 1200
2. Keyboard — 2500
3. Headphones — 3000
4. Office Chair — 8500
5. Desk — 12000
6. Laptop — 50000

Actual order matched the expected order.

Result: **PASS**

### TC-SQL-008 — Valid User References

```sql
SELECT orders.*
FROM orders
LEFT JOIN users
    ON orders.user_id = users.id
WHERE users.id IS NULL;
```

Expected: 0 orphan orders.

Actual: 0 orphan orders.

Result: **PASS**

### TC-SQL-010 — Valid Product References

```sql
SELECT order_items.*
FROM order_items
LEFT JOIN products
    ON order_items.product_id = products.id
WHERE products.id IS NULL;
```

Expected: 0 orphan order items.

Actual: 0 orphan order items.

Result: **PASS**

## Summary

- Total executed: 5
- Passed: 5
- Failed: 0
- Blocked: 0
- Pass rate: 100%

These results were generated by executing the queries against the local practice SQLite database included with this project.
